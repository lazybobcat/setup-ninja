-- This file will be moved, but for dev purposes it is here.
return {
  terminal = { arch = "ghostty", fedora = "ghostty", debian = "ghostty", description = "Terminal emulator" },
  ssh = { arch = "openssh", fedora = "openssh", debian = "openssh", description = "SSH client" },
}
